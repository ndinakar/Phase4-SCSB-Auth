package org.recap.security;

import org.apache.shiro.authc.AuthenticationInfo;
import org.apache.shiro.authc.AuthenticationToken;
import org.junit.Before;
import org.junit.Test;

import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
import org.mockito.junit.MockitoJUnitRunner;
import org.powermock.api.mockito.PowerMockito;
import org.recap.security.realm.SimpleAuthorizationRealm;
import org.springframework.test.util.ReflectionTestUtils;

import static org.mockito.ArgumentMatchers.eq;


//@RunWith(MockitoJUnitRunner.class)
public class SimpleAuthorizationRealmUT{
/*@InjectMocks
SimpleAuthorizationRealm simpleAuthorizationRelam;*/

   /* @Mock
    private AuthenticationService authenticationService;

    @Mock
    private AuthorizationService authorizationService;

    @Mock
    AuthenticationInfo authenticationInfo;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
    }
*/
    /*@Test
    public void doGetAuthenticationInfo( ) throws Exception {
        String loginUser="john:CUL";
        AuthenticationToken authenticationToken = null;
        String testMethod = "doGetAuthenticationInfo";
        SimpleAuthorizationRealm mockObject = PowerMockito.mock(SimpleAuthorizationRealm.class);
        PowerMockito.doReturn( authenticationInfo ).when( mockObject, testMethod, eq(authenticationToken) );

    }*/
}
